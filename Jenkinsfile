pipeline {
  agent any

  environment {
    NODE_VERSION = '24'
    DOCKER_IMAGE = 'node:${NODE_VERSION}-alpine'
  }

  stages {
    stage('Checkout') {
        steps {
          git branch: 'main', url: 
        }
      }
  }
}